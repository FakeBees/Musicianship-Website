# School System — UI & Navigation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the complete school/user system UI: role-specific nav dropdowns, student My Classes page, home centering fix, teacher class improvements (hide modules, add modules/exercises, fix back buttons), admin-teacher "Admin" shortcut to their school, teacher home, course management UI (manage buttons, rename/duplicate), school details (join code, student/teacher columns, role assignment), user management for full admin, school join flow, and class_teacher role assignment.

**Architecture:** Flask server-rendered Jinja2 templates. No JS frameworks — plain Bootstrap 5 forms and tables. Each subsystem adds routes + templates. Depends on `2026-06-28-school-schema.md` being fully applied first (`admin_teacher` role, `SchoolMembership`, `School.join_code`, `Class.assigned_teacher_id` all in DB).

**Tech Stack:** Flask, SQLAlchemy, Jinja2, Bootstrap 5

## Global Constraints

- Prerequisite: `2026-06-28-school-schema.md` tasks 1–3 must be complete before starting this plan.
- Phase 3 admin CMS (`/admin/melodies`, `/admin/harmonics`, `/admin/rhythms`, `/admin/holistic`, `/admin/gen-progressions`, `/admin/melodies/generate`) stays `@role_required('admin')` only — do NOT add `admin_teacher` access.
- No JavaScript beyond existing Bootstrap and inline `onsubmit="return confirm(...)"` patterns.
- All new templates extend `base.html`.
- School join code is plaintext 8-char alphanumeric for now (temporary, noted for future security improvement).
- `class_teacher` role: can view and manage only their assigned class. Cannot create new classes.

---

### Task 1: Nav dropdown restructure per role

**Files:**
- Modify: `templates/base.html`

**Spec:**
- **Student** dropdown: My Progress → `/me` | My Classes → `/my-classes` | Home → `/` | Log out
- **admin_teacher** dropdown: My Classes → `/teacher` | Admin → `/admin/my-school` | Home → `/` | My Progress → `/me` | Log out
- **class_teacher** dropdown: My Classes → `/teacher` | Home → `/` | My Progress → `/me` | Log out
- **admin** dropdown: My Classes → `/teacher` | Admin → `/admin` | Home → `/` | My Progress → `/me` | Log out
- Remove the per-class links from the dropdown (they were the looped `{% for klass in current_user.classes %}` block)

- [ ] **Step 1: Update `base.html` nav dropdown**

Replace the entire `{% if current_user.is_authenticated %}` dropdown block (currently lines 29–56) with:

```html
{% if current_user.is_authenticated %}
<li class="nav-item dropdown">
  <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
    {{ current_user.display_name or current_user.email }}
  </a>
  <ul class="dropdown-menu dropdown-menu-end">
    {% if current_user.role == 'student' %}
      <li><a class="dropdown-item" href="{{ url_for('me') }}">My Progress</a></li>
      <li><a class="dropdown-item" href="{{ url_for('my_classes') }}">My Classes</a></li>
      <li><a class="dropdown-item" href="{{ url_for('home') }}">Home</a></li>
    {% elif current_user.role in ('admin_teacher', 'class_teacher') %}
      <li><a class="dropdown-item" href="{{ url_for('teacher_dashboard') }}">My Classes</a></li>
      {% if current_user.role == 'admin_teacher' %}
      <li><a class="dropdown-item" href="{{ url_for('admin_my_school') }}">Admin</a></li>
      {% endif %}
      <li><a class="dropdown-item" href="{{ url_for('home') }}">Home</a></li>
      <li><a class="dropdown-item" href="{{ url_for('me') }}">My Progress</a></li>
    {% elif current_user.role == 'admin' %}
      <li><a class="dropdown-item" href="{{ url_for('teacher_dashboard') }}">My Classes</a></li>
      <li><a class="dropdown-item" href="{{ url_for('admin') }}">Admin</a></li>
      <li><a class="dropdown-item" href="{{ url_for('home') }}">Home</a></li>
      <li><a class="dropdown-item" href="{{ url_for('me') }}">My Progress</a></li>
    {% endif %}
    <li><hr class="dropdown-divider"></li>
    <li><a class="dropdown-item" href="{{ url_for('logout') }}">Log out</a></li>
  </ul>
</li>
{% else %}
<li class="nav-item"><a class="nav-link" href="{{ url_for('login') }}">Log in</a></li>
<li class="nav-item"><a class="nav-link" href="{{ url_for('register') }}">Register</a></li>
{% endif %}
```

- [ ] **Step 2: Add stub route `admin_my_school` to `app.py`** (full implementation in Task 7)

Add after the existing `teacher_dashboard` route:

```python
@app.route('/admin/my-school')
@login_required
@role_required('admin_teacher', 'admin')
def admin_my_school():
    # find school where current user is an admin_teacher member
    mem = SchoolMembership.query.filter_by(
        user_id=current_user.id, role='admin_teacher'
    ).first()
    if mem:
        return redirect(url_for('admin_courses', school_id=mem.school_id))
    # full admins: redirect to schools list
    return redirect(url_for('admin_schools'))
```

Also add to the imports at the top of `app.py` (if not already present):
```python
from models import db, ..., SchoolMembership
```

- [ ] **Step 3: Add stub route `my_classes` to `app.py`** (full implementation in Task 2)

```python
@app.route('/my-classes')
@login_required
def my_classes():
    return render_template('student/my_classes.html',
                           classes=current_user.classes)
```

- [ ] **Step 4: Smoke-test nav renders for each role**

Start the dev server (`python app.py`) and log in as:
- A student account: verify dropdown shows My Progress / My Classes / Home / Log out
- An admin_teacher account: verify My Classes / Admin / Home / My Progress / Log out
- An admin account: verify My Classes / Admin / Home / My Progress / Log out

- [ ] **Step 5: Commit**

```bash
git add templates/base.html app.py
git commit -m "feat: role-specific nav dropdown (student, admin_teacher, class_teacher, admin)"
```

---

### Task 2: Student My Classes page + strip join/leave from /me

**Files:**
- Create: `templates/student/my_classes.html`
- Modify: `templates/me.html`
- Modify: `app.py` (`my_classes` route — replace stub with full implementation; update `leave_class` redirect)

**Spec:**
- `/my-classes`: join new class form at top; list of enrolled classes with Leave button (confirmation); no roster view.
- `/me`: remove the "Join a class" form and the "Your classes" list entirely — keep only stats and module progress.
- Leave confirmation: `onsubmit="return confirm('Leave class \"{{ klass.name }}\"? You will lose access to its modules.')"`.

- [ ] **Step 1: Create `templates/student/my_classes.html`**

```html
{% extends 'base.html' %}
{% block title %}My Classes{% endblock %}
{% block content %}
<div class="container py-4" style="max-width:700px">
  <h2 class="mb-4">My Classes</h2>

  {% with messages = get_flashed_messages(with_categories=true) %}
    {% for cat, msg in messages %}<div class="alert alert-{{ cat }}">{{ msg }}</div>{% endfor %}
  {% endwith %}

  <div class="card mb-4">
    <div class="card-header fw-semibold">Join a class</div>
    <div class="card-body">
      <form method="post" action="{{ url_for('join_class') }}" class="d-flex gap-2" style="max-width:360px">
        <input type="text" name="join_code" class="form-control" placeholder="Enter class join code" required>
        <button type="submit" class="btn btn-outline-primary">Join</button>
      </form>
    </div>
  </div>

  {% if classes %}
  <h5 class="mb-3">Your classes</h5>
  <div class="list-group">
    {% for klass in classes %}
    <div class="list-group-item d-flex justify-content-between align-items-center">
      <div>
        <strong>{{ klass.name }}</strong>
        {% if klass.course %}<span class="text-muted small ms-2">{{ klass.course.name }}</span>{% endif %}
      </div>
      <form method="post" action="{{ url_for('leave_class', class_id=klass.id) }}"
            onsubmit="return confirm('Leave class &quot;{{ klass.name }}&quot;? You will lose access to its modules.')">
        <button class="btn btn-sm btn-outline-danger">Leave</button>
      </form>
    </div>
    {% endfor %}
  </div>
  {% else %}
  <p class="text-muted">You are not enrolled in any classes yet.</p>
  {% endif %}
</div>
{% endblock %}
```

- [ ] **Step 2: Update `app.py` — replace `my_classes` stub with full route**

```python
@app.route('/my-classes')
@login_required
def my_classes():
    return render_template('student/my_classes.html',
                           classes=current_user.classes)
```

(The stub from Task 1 is already this — confirm it's correct, no change needed if identical.)

- [ ] **Step 3: Update `leave_class` redirect target**

In `app.py`, change the `leave_class` route's redirect from `url_for('me')` to `url_for('my_classes')`:

```python
return redirect(url_for('my_classes'))
```

- [ ] **Step 4: Remove join/leave section from `me.html`**

In `templates/me.html`, remove the entire block that starts with:
```html
  <!-- Join a class -->
  <hr>
  <h5>Join a class</h5>
```
through the end of the `{% if current_user.classes %}` block that lists classes with leave buttons (including the closing `{% endif %}`).

- [ ] **Step 5: Verify manually**

Visit `/my-classes` as a student: join form and class list with Leave buttons visible.
Visit `/me` as a student: no join form, no class list — only stats and module progress.

- [ ] **Step 6: Commit**

```bash
git add templates/student/my_classes.html templates/me.html app.py
git commit -m "feat: student My Classes page; strip join/leave from /me"
```

---

### Task 3: Home page single-class centering fix

**Files:**
- Modify: `templates/home.html`

**Spec:** If the user is in exactly one class, that class card should be centered on the page, not left-aligned. The current code wraps each class in `<div class="col-md-6">` inside a `<div class="row g-4">` — with one card that's left-aligned.

- [ ] **Step 1: Fix the row wrapping in `home.html`**

Find the class selector section in `home.html`:
```html
    <div class="row g-4 mb-4">
      {% for klass in user_classes %}
      <div class="col-md-6">
```

Replace with:
```html
    <div class="row g-4 mb-4 {% if user_classes | length == 1 %}justify-content-center{% endif %}">
      {% for klass in user_classes %}
      <div class="col-md-6">
```

- [ ] **Step 2: Verify manually**

Log in as a student with exactly one class: the card should be centered. Log in as a student with two classes: two cards side by side (unchanged).

- [ ] **Step 3: Commit**

```bash
git add templates/home.html
git commit -m "fix: center single class card on home page"
```

---

### Task 4: Teacher edit class — fix back buttons + hide whole modules + add exercises

**Files:**
- Modify: `templates/teacher/edit_class.html`
- Modify: `app.py` (add `teacher_hide_module` route; add `teacher_add_module_exercise` route)

**Spec:**
- Back buttons say "Back to Classes" (linking to `/teacher`) — not "Back to Roster" or "← Roster"
- Can hide whole modules (a new "Hide module" button per module header; restores all exercises in it)
- Can add module exercises from within the teacher edit view (new form at bottom of each module section)
- Hiding a module hides all its exercises for the class (server creates override rows for each exercise)

**New routes:**
- `POST /teacher/classes/<class_id>/modules/<module_id>/hide` → creates `hide` overrides for all exercises in that module
- `POST /teacher/classes/<class_id>/modules/<module_id>/restore` → deletes all hide overrides for that module
- `POST /teacher/classes/<class_id>/module_exercises/add` → creates a new `ModuleExercise` (add form with exercise_type, exercise_id, name, order)

- [ ] **Step 1: Add routes to `app.py`**

Add after `teacher_delete_override`:

```python
@app.route('/teacher/classes/<int:class_id>/modules/<int:module_id>/hide', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def teacher_hide_module(class_id, module_id):
    klass = Class.query.get_or_404(class_id)
    module = Module.query.get_or_404(module_id)
    for ex in module.exercises:
        exists = ClassModuleExercise.query.filter_by(
            class_id=class_id, module_exercise_id=ex.id, action='hide'
        ).first()
        if not exists:
            db.session.add(ClassModuleExercise(
                class_id=class_id,
                module_id=module_id,
                module_exercise_id=ex.id,
                action='hide'
            ))
    db.session.commit()
    flash(f'Module "{module.name}" hidden for this class.', 'success')
    return redirect(url_for('teacher_edit_class', class_id=class_id))


@app.route('/teacher/classes/<int:class_id>/modules/<int:module_id>/restore', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def teacher_restore_module(class_id, module_id):
    ClassModuleExercise.query.filter_by(
        class_id=class_id, module_id=module_id, action='hide'
    ).delete()
    db.session.commit()
    flash('Module restored.', 'success')
    return redirect(url_for('teacher_edit_class', class_id=class_id))


@app.route('/teacher/classes/<int:class_id>/module_exercises/add', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def teacher_add_module_exercise(class_id):
    module_id    = request.form.get('module_id', type=int)
    exercise_type = request.form.get('exercise_type', '').strip()
    exercise_id  = request.form.get('exercise_id', type=int)
    name         = request.form.get('name', '').strip()
    if not all([module_id, exercise_type, exercise_id]):
        flash('All fields required.', 'danger')
        return redirect(url_for('teacher_edit_class', class_id=class_id))
    last = ModuleExercise.query.filter_by(module_id=module_id).order_by(
        ModuleExercise.order.desc()
    ).first()
    order = (last.order + 1) if last else 0
    me = ModuleExercise(
        module_id=module_id,
        exercise_type=exercise_type,
        exercise_id=exercise_id,
        name=name or exercise_type,
        order=order,
    )
    db.session.add(me)
    db.session.commit()
    flash(f'Exercise "{me.name}" added to module.', 'success')
    return redirect(url_for('teacher_edit_class', class_id=class_id))
```

- [ ] **Step 2: Check what model `ClassModuleExercise` is called in the codebase**

```bash
grep -n "ClassModuleExercise\|class_module_exercise\|ClassOverride" /Users/jareddamron/PycharmProjects/Musicianship/models.py | head -20
```

Use whatever name the existing override model has (it may be `ClassModuleExercise` or `ClassOverride`). Update the route code above to use the correct model name.

- [ ] **Step 3: Update `templates/teacher/edit_class.html`**

Make three changes:

**a) Fix back buttons** — find `← Roster` and `Back to Roster` and change to `← Classes` / `Back to Classes` pointing to `url_for('teacher_dashboard')`:

Old:
```html
<a href="{{ url_for('teacher_class_detail', class_id=cls.id) }}" class="btn btn-link p-0 text-decoration-none">← Roster</a>
```
New:
```html
<a href="{{ url_for('teacher_dashboard') }}" class="btn btn-link p-0 text-decoration-none">← Classes</a>
```

Old (footer):
```html
<a href="{{ url_for('teacher_class_detail', class_id=cls.id) }}" class="btn btn-outline-secondary">Back to Roster</a>
```
New:
```html
<a href="{{ url_for('teacher_dashboard') }}" class="btn btn-outline-secondary">Back to Classes</a>
```

**b) Add module-level Hide/Restore buttons** — in the `{% for item in modules_with_exercises %}` loop, add before the module's exercise list:

```html
<div class="d-flex align-items-center justify-content-between mt-3 mb-2">
  <h6 class="fw-semibold mb-0">{{ item.module.name }}</h6>
  {% set all_hidden = item.exercises | length > 0 and item.exercises | selectattr('id', 'in', hidden_ids) | list | length == item.exercises | length %}
  {% if all_hidden %}
  <form method="post" action="{{ url_for('teacher_restore_module', class_id=cls.id, module_id=item.module.id) }}">
    <button class="btn btn-sm btn-outline-success">Restore module</button>
  </form>
  {% else %}
  <form method="post" action="{{ url_for('teacher_hide_module', class_id=cls.id, module_id=item.module.id) }}">
    <button class="btn btn-sm btn-outline-warning">Hide module</button>
  </form>
  {% endif %}
</div>
```

Remove the old bare `<h6 class="fw-semibold mt-3 mb-2">{{ item.module.name }}</h6>` line.

**c) Add "Add exercise" form at the bottom of each module section**, after the `{% if item.exercises %}...{% endif %}` block:

```html
<details class="mt-2 mb-3">
  <summary class="text-muted small" style="cursor:pointer">+ Add exercise to this module</summary>
  <form method="post" action="{{ url_for('teacher_add_module_exercise', class_id=cls.id) }}" class="mt-2 p-3 border rounded bg-light">
    <input type="hidden" name="module_id" value="{{ item.module.id }}">
    <div class="row g-2">
      <div class="col-md-3">
        <select name="exercise_type" class="form-select form-select-sm" required>
          <option value="">Type</option>
          <option value="melody">Melody</option>
          <option value="rhythm">Rhythm</option>
          <option value="harmonic">Harmonic</option>
          <option value="holistic">Holistic</option>
        </select>
      </div>
      <div class="col-md-3">
        <input type="number" name="exercise_id" class="form-control form-control-sm" placeholder="Exercise ID" required>
      </div>
      <div class="col-md-4">
        <input type="text" name="name" class="form-control form-control-sm" placeholder="Display name (optional)">
      </div>
      <div class="col-md-2">
        <button type="submit" class="btn btn-sm btn-primary w-100">Add</button>
      </div>
    </div>
  </form>
</details>
```

- [ ] **Step 4: Verify manually**

Open edit class view: back button says "← Classes". Module headers have Hide/Restore buttons. Each module has an "Add exercise" details/summary section.

- [ ] **Step 5: Commit**

```bash
git add app.py templates/teacher/edit_class.html
git commit -m "feat: teacher edit class — fix back buttons, hide/restore modules, add exercises"
```

---

### Task 5: Teacher dashboard — class_teacher access to assigned classes

**Files:**
- Modify: `app.py` (`teacher_dashboard`, `teacher_class_detail`, `teacher_edit_class`)
- Modify: `templates/teacher/dashboard.html`

**Spec:** `class_teacher` can view `/teacher` and see only their assigned classes. They cannot create new classes. Admin/admin_teacher see all their created classes.

- [ ] **Step 1: Update `teacher_dashboard` route**

```python
@app.route('/teacher')
@login_required
@role_required('admin_teacher', 'class_teacher', 'admin')
def teacher_dashboard():
    if current_user.role == 'class_teacher':
        classes = Class.query.filter_by(
            assigned_teacher_id=current_user.id
        ).all()
    else:
        classes = Class.query.filter_by(
            teacher_id=current_user.id
        ).all()
    return render_template('teacher/dashboard.html', classes=classes)
```

- [ ] **Step 2: Guard class-detail and edit routes against cross-access**

In `teacher_class_detail` and `teacher_edit_class`, after fetching the class, add an ownership check:

```python
cls = Class.query.get_or_404(class_id)
if current_user.role == 'class_teacher' and cls.assigned_teacher_id != current_user.id:
    abort(403)
if current_user.role == 'admin_teacher' and cls.teacher_id != current_user.id:
    abort(403)
```

- [ ] **Step 3: Update `dashboard.html` to hide "New class" for class_teachers**

```html
{% if current_user.role != 'class_teacher' %}
<a href="{{ url_for('teacher_new_class') }}" class="btn btn-primary">+ New class</a>
{% endif %}
```

- [ ] **Step 4: Update `role_required` on all teacher routes to accept `class_teacher`**

Routes that `class_teacher` should access: `teacher_dashboard`, `teacher_class_detail`, `teacher_edit_class`, `teacher_add_override`, `teacher_delete_override`, `teacher_hide_module`, `teacher_restore_module`, `teacher_add_module_exercise`, `teacher_kick_student`.

Routes that `class_teacher` should NOT access: `teacher_new_class`, `teacher_delete_class`, `teacher_set_course`.

Update decorators accordingly:
- Routes class_teacher CAN access: `@role_required('admin_teacher', 'class_teacher', 'admin')`
- Routes class_teacher CANNOT access: keep `@role_required('admin_teacher', 'admin')`

- [ ] **Step 5: Commit**

```bash
git add app.py templates/teacher/dashboard.html
git commit -m "feat: class_teacher role access to assigned classes in teacher dashboard"
```

---

### Task 6: Course management UI — manage buttons, rename, duplicate

**Files:**
- Modify: `templates/admin/courses.html`
- Modify: `templates/admin/schools.html`
- Modify: `app.py` (add `admin_rename_course`, `admin_duplicate_course` routes)

**Spec:**
- In course table: replace `[Modules →]` button with "Manage" next to module count; add "Manage" next to class count (links to class management in school view — for now link to `/teacher` filtered, or just show inline class list — actually: "Manage" for classes links to a new `/admin/courses/<id>/classes` page, or simplest: link to `admin_modules` page — NO, keep it simple: "Manage" by classes count just opens the existing `/teacher` dashboard for now, since class management is teacher-facing. Actually the spec says manage classes and modules from the course view. So "Manage" next to module count → `admin_modules`. "Manage" next to class count → new route `admin_course_classes` that shows classes using that course).
- Add Rename button (inline form) and Duplicate button.
- In schools table: move `[Courses →]` to a "Manage" button next to courses count.

**New routes:**
- `POST /admin/courses/<id>/rename` — rename a course
- `POST /admin/courses/<id>/duplicate` — duplicate course + all its modules + all module exercises
- `GET /admin/courses/<id>/classes` — list classes using this course (with ability to set/unset course)

- [ ] **Step 1: Add routes to `app.py`**

Add after `admin_delete_course`:

```python
@app.route('/admin/courses/<int:course_id>/rename', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def admin_rename_course(course_id):
    course = Course.query.get_or_404(course_id)
    name = request.form.get('name', '').strip()
    if name:
        course.name = name
        db.session.commit()
        flash(f'Course renamed to "{name}".', 'success')
    return redirect(url_for('admin_courses', school_id=course.school_id))


@app.route('/admin/courses/<int:course_id>/duplicate', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def admin_duplicate_course(course_id):
    src = Course.query.get_or_404(course_id)
    new_course = Course(name=f'{src.name} (copy)', school_id=src.school_id)
    db.session.add(new_course)
    db.session.flush()
    for mod in src.modules.order_by(Module.order):
        new_mod = Module(name=mod.name, course_id=new_course.id, order=mod.order)
        db.session.add(new_mod)
        db.session.flush()
        for ex in mod.exercises:
            new_ex = ModuleExercise(
                module_id=new_mod.id,
                exercise_type=ex.exercise_type,
                exercise_id=ex.exercise_id,
                name=ex.name,
                params_json=ex.params_json,
                order=ex.order,
                completion_criterion_json=ex.completion_criterion_json,
            )
            db.session.add(new_ex)
    db.session.commit()
    flash(f'Duplicated "{src.name}".', 'success')
    return redirect(url_for('admin_courses', school_id=src.school_id))


@app.route('/admin/courses/<int:course_id>/classes')
@login_required
@role_required('admin_teacher', 'admin')
def admin_course_classes(course_id):
    course = Course.query.get_or_404(course_id)
    classes = Class.query.filter_by(course_id=course_id).all()
    return render_template('admin/course_classes.html',
                           course=course, classes=classes)
```

- [ ] **Step 2: Update `templates/admin/courses.html`**

Replace the courses table with:

```html
<table class="table table-sm align-middle">
  <thead><tr><th>Name</th><th>Modules</th><th>Classes</th><th></th></tr></thead>
  <tbody>
  {% for c in courses %}
  <tr>
    <td>{{ c.name }}</td>
    <td>
      {{ c.modules.count() }}
      <a href="{{ url_for('admin_modules', course_id=c.id) }}" class="btn btn-sm btn-outline-secondary ms-1">Manage</a>
    </td>
    <td>
      {{ c.classes | length }}
      <a href="{{ url_for('admin_course_classes', course_id=c.id) }}" class="btn btn-sm btn-outline-secondary ms-1">Manage</a>
    </td>
    <td class="d-flex gap-1 flex-wrap">
      <button class="btn btn-sm btn-outline-secondary" data-bs-toggle="collapse"
              data-bs-target="#rename-{{ c.id }}">Rename</button>
      <form method="post" action="{{ url_for('admin_duplicate_course', course_id=c.id) }}"
            onsubmit="return confirm('Duplicate course &quot;{{ c.name }}&quot;?')">
        <button class="btn btn-sm btn-outline-secondary">Duplicate</button>
      </form>
      <form method="post" action="{{ url_for('admin_delete_course', course_id=c.id) }}"
            onsubmit="return confirm('Delete course &quot;{{ c.name }}&quot; and all its modules?')">
        <button class="btn btn-sm btn-outline-danger">Delete</button>
      </form>
    </td>
  </tr>
  <tr class="collapse" id="rename-{{ c.id }}">
    <td colspan="4">
      <form method="post" action="{{ url_for('admin_rename_course', course_id=c.id) }}"
            class="d-flex gap-2" style="max-width:360px">
        <input name="name" class="form-control form-control-sm" value="{{ c.name }}" required>
        <button class="btn btn-sm btn-primary">Save</button>
      </form>
    </td>
  </tr>
  {% else %}
  <tr><td colspan="4" class="text-muted">No courses yet.</td></tr>
  {% endfor %}
  </tbody>
</table>
```

- [ ] **Step 3: Update `templates/admin/schools.html`**

Replace the schools table `<td>{{ s.courses.count() }}</td><td class="d-flex gap-1"><a href="...">Courses →</a>` with:

```html
<td>
  {{ s.courses.count() }}
  <a href="{{ url_for('admin_courses', school_id=s.id) }}" class="btn btn-sm btn-outline-secondary ms-1">Manage</a>
</td>
```

Remove the separate `<a href="{{ url_for('admin_courses', school_id=s.id) }}" class="btn btn-sm btn-outline-secondary">Courses →</a>` from the action column.

- [ ] **Step 4: Create `templates/admin/course_classes.html`**

```html
{% extends 'base.html' %}
{% block title %}{{ course.name }} — Classes{% endblock %}
{% block content %}
<div class="container py-4">
  <h2>{{ course.name }} — Classes</h2>
  <a href="{{ url_for('admin_courses', school_id=course.school_id) }}" class="btn btn-outline-secondary btn-sm mb-3">← Courses</a>
  <table class="table table-sm">
    <thead><tr><th>Class</th><th>Students</th><th>Teacher</th></tr></thead>
    <tbody>
    {% for cls in classes %}
    <tr>
      <td>{{ cls.name }} <code class="text-muted small">{{ cls.join_code }}</code></td>
      <td>{{ cls.members | length }}</td>
      <td>{{ cls.teacher.display_name or cls.teacher.email }}</td>
    </tr>
    {% else %}
    <tr><td colspan="3" class="text-muted">No classes use this course.</td></tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %}
```

- [ ] **Step 5: Verify manually**

Visit `/admin/schools` → schools table has "Manage" next to courses count (no separate Courses → button).
Visit a school's courses page → each course has Manage/Rename/Duplicate/Delete; Rename shows collapse; Manage by modules links to modules; Manage by classes links to class list.

- [ ] **Step 6: Commit**

```bash
git add app.py templates/admin/courses.html templates/admin/schools.html templates/admin/course_classes.html
git commit -m "feat: course management UI — manage buttons, rename, duplicate; schools manage button"
```

---

### Task 7: School details — join code, student/teacher counts, role assignment

**Files:**
- Modify: `templates/admin/schools.html` (school table + detail expansion)
- Create: `templates/admin/school_detail.html`
- Modify: `app.py` (add `admin_school_detail`, `admin_set_user_role`, `admin_teacher_set_member_role` routes)

**Spec:**
- School table (full admin): show columns for student count, teacher count, join code
- "Manage" link per school opens school detail page showing all members with roles
- Full admin: can set any user (searched by email) as `admin_teacher` or `class_teacher` for a school
- admin_teacher: can set any *current school member* as `admin_teacher` or `class_teacher`
- Show and allow regenerating the join code (simple button — generates new random 8-char code)

**New routes:**
- `GET/POST /admin/schools/<id>/detail` — school detail page
- `POST /admin/schools/<id>/set-member-role` — set a member's school role
- `POST /admin/schools/<id>/add-member` — add user to school by email (admin only)
- `POST /admin/schools/<id>/regen-join-code` — regenerate join code

- [ ] **Step 1: Update schools table in `schools.html`**

Expand the table header and rows:

```html
<table class="table table-sm align-middle">
  <thead>
    <tr>
      <th>Name</th>
      <th>Courses</th>
      <th>Students</th>
      <th>Teachers</th>
      <th>Join Code</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  {% for s in schools %}
  {% set members = s.memberships.all() %}
  <tr>
    <td>{{ s.name }}</td>
    <td>
      {{ s.courses.count() }}
      <a href="{{ url_for('admin_courses', school_id=s.id) }}" class="btn btn-sm btn-outline-secondary ms-1">Manage</a>
    </td>
    <td>{{ members | selectattr('role', 'equalto', 'student') | list | length }}</td>
    <td>{{ members | rejectattr('role', 'equalto', 'student') | list | length }}</td>
    <td><code>{{ s.join_code or '—' }}</code></td>
    <td class="d-flex gap-1">
      <a href="{{ url_for('admin_school_detail', school_id=s.id) }}" class="btn btn-sm btn-outline-primary">Details</a>
      <form method="post" action="{{ url_for('admin_delete_school', school_id=s.id) }}"
            onsubmit="return confirm('Delete school &quot;{{ s.name }}&quot; and all its courses?')">
        <button class="btn btn-sm btn-outline-danger">Delete</button>
      </form>
    </td>
  </tr>
  {% else %}
  <tr><td colspan="6" class="text-muted">No schools yet.</td></tr>
  {% endfor %}
  </tbody>
</table>
```

- [ ] **Step 2: Add routes to `app.py`**

```python
import secrets, string as _string
_ALPHA = _string.ascii_uppercase + _string.digits

def _random_school_code():
    return ''.join(secrets.choice(_ALPHA) for _ in range(8))


@app.route('/admin/schools/<int:school_id>/detail', methods=['GET'])
@login_required
@role_required('admin_teacher', 'admin')
def admin_school_detail(school_id):
    school = School.query.get_or_404(school_id)
    # admin_teacher may only view their own school
    if current_user.role == 'admin_teacher':
        mem = SchoolMembership.query.filter_by(
            school_id=school_id, user_id=current_user.id, role='admin_teacher'
        ).first()
        if not mem:
            abort(403)
    memberships = SchoolMembership.query.filter_by(school_id=school_id).all()
    return render_template('admin/school_detail.html',
                           school=school,
                           memberships=memberships,
                           is_full_admin=(current_user.role == 'admin'))


@app.route('/admin/schools/<int:school_id>/set-member-role', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def admin_set_member_role(school_id):
    school = School.query.get_or_404(school_id)
    user_id  = request.form.get('user_id', type=int)
    new_role = request.form.get('role', '').strip()
    if new_role not in ('student', 'admin_teacher', 'class_teacher'):
        flash('Invalid role.', 'danger')
        return redirect(url_for('admin_school_detail', school_id=school_id))
    # admin_teacher may only promote existing members
    if current_user.role == 'admin_teacher':
        mem = SchoolMembership.query.filter_by(
            school_id=school_id, user_id=user_id
        ).first_or_404()
    else:
        mem = SchoolMembership.query.filter_by(
            school_id=school_id, user_id=user_id
        ).first()
        if not mem:
            flash('User is not a member of this school.', 'danger')
            return redirect(url_for('admin_school_detail', school_id=school_id))
    mem.role = new_role
    # mirror onto User.role if promoting to admin_teacher or class_teacher
    if new_role in ('admin_teacher', 'class_teacher'):
        target = User.query.get(user_id)
        if target and target.role == 'student':
            target.role = new_role
    db.session.commit()
    flash('Role updated.', 'success')
    return redirect(url_for('admin_school_detail', school_id=school_id))


@app.route('/admin/schools/<int:school_id>/add-member', methods=['POST'])
@login_required
@role_required('admin')
def admin_add_school_member(school_id):
    school = School.query.get_or_404(school_id)
    email   = request.form.get('email', '').strip()
    role    = request.form.get('role', 'student')
    if role not in ('student', 'admin_teacher', 'class_teacher'):
        role = 'student'
    user = User.query.filter_by(email=email).first()
    if not user:
        flash(f'No user with email "{email}".', 'danger')
        return redirect(url_for('admin_school_detail', school_id=school_id))
    exists = SchoolMembership.query.filter_by(
        school_id=school_id, user_id=user.id
    ).first()
    if exists:
        flash(f'{email} is already a member.', 'info')
        return redirect(url_for('admin_school_detail', school_id=school_id))
    db.session.add(SchoolMembership(school_id=school_id, user_id=user.id, role=role))
    if role in ('admin_teacher', 'class_teacher') and user.role == 'student':
        user.role = role
    db.session.commit()
    flash(f'Added {email} as {role}.', 'success')
    return redirect(url_for('admin_school_detail', school_id=school_id))


@app.route('/admin/schools/<int:school_id>/regen-join-code', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def admin_regen_join_code(school_id):
    school = School.query.get_or_404(school_id)
    if current_user.role == 'admin_teacher':
        mem = SchoolMembership.query.filter_by(
            school_id=school_id, user_id=current_user.id, role='admin_teacher'
        ).first()
        if not mem:
            abort(403)
    school.join_code = _random_school_code()
    db.session.commit()
    flash(f'Join code regenerated: {school.join_code}', 'success')
    return redirect(url_for('admin_school_detail', school_id=school_id))
```

- [ ] **Step 3: Create `templates/admin/school_detail.html`**

```html
{% extends 'base.html' %}
{% block title %}{{ school.name }} — Details{% endblock %}
{% block content %}
<div class="container py-4">
  <h2>{{ school.name }}</h2>
  <a href="{{ url_for('admin_schools') }}" class="btn btn-outline-secondary btn-sm mb-3">← Schools</a>

  <!-- Join code -->
  <div class="card mb-4">
    <div class="card-header fw-semibold">School Join Code</div>
    <div class="card-body d-flex align-items-center gap-3">
      <code class="fs-5">{{ school.join_code or '—' }}</code>
      <form method="post" action="{{ url_for('admin_regen_join_code', school_id=school.id) }}"
            onsubmit="return confirm('Regenerate join code? The old code will stop working.')">
        <button class="btn btn-sm btn-outline-secondary">Regenerate</button>
      </form>
    </div>
  </div>

  <!-- Add member (full admin only) -->
  {% if is_full_admin %}
  <div class="card mb-4">
    <div class="card-header fw-semibold">Add member by email</div>
    <div class="card-body">
      <form method="post" action="{{ url_for('admin_add_school_member', school_id=school.id) }}"
            class="d-flex gap-2 flex-wrap">
        <input name="email" type="email" class="form-control" placeholder="user@example.com"
               style="max-width:280px" required>
        <select name="role" class="form-select" style="max-width:180px">
          <option value="student">Student</option>
          <option value="class_teacher">Class Teacher</option>
          <option value="admin_teacher">Admin Teacher</option>
        </select>
        <button class="btn btn-primary">Add</button>
      </form>
    </div>
  </div>
  {% endif %}

  <!-- Members table -->
  {% with messages = get_flashed_messages(with_categories=true) %}
    {% for cat, msg in messages %}<div class="alert alert-{{ cat }}">{{ msg }}</div>{% endfor %}
  {% endwith %}
  <table class="table table-sm align-middle">
    <thead>
      <tr><th>Email</th><th>Name</th><th>Role</th><th>Joined</th><th></th></tr>
    </thead>
    <tbody>
    {% for m in memberships %}
    <tr>
      <td>{{ m.user.email }}</td>
      <td>{{ m.user.display_name or '—' }}</td>
      <td><span class="badge bg-secondary">{{ m.role }}</span></td>
      <td class="text-muted small">{{ m.joined_at.strftime('%Y-%m-%d') if m.joined_at else '—' }}</td>
      <td>
        <form method="post" action="{{ url_for('admin_set_member_role', school_id=school.id) }}"
              class="d-flex gap-1">
          <input type="hidden" name="user_id" value="{{ m.user_id }}">
          <select name="role" class="form-select form-select-sm" style="width:auto">
            {% if is_full_admin %}
            <option value="student" {% if m.role == 'student' %}selected{% endif %}>Student</option>
            {% endif %}
            <option value="class_teacher" {% if m.role == 'class_teacher' %}selected{% endif %}>Class Teacher</option>
            <option value="admin_teacher" {% if m.role == 'admin_teacher' %}selected{% endif %}>Admin Teacher</option>
          </select>
          <button class="btn btn-sm btn-outline-primary">Set</button>
        </form>
      </td>
    </tr>
    {% else %}
    <tr><td colspan="5" class="text-muted">No members yet.</td></tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %}
```

- [ ] **Step 4: Verify manually**

Visit `/admin/schools` — table shows student count, teacher count, join code columns.
Click Details for a school — join code shown with Regenerate button; member list with role assignment dropdowns.
Full admin: "Add member by email" form visible. Admin teacher: form hidden.

- [ ] **Step 5: Commit**

```bash
git add app.py templates/admin/schools.html templates/admin/school_detail.html
git commit -m "feat: school details — join code, member counts, role assignment"
```

---

### Task 8: School join flow — join school before joining a class

**Files:**
- Modify: `app.py` (`join_class` route)
- Create: `templates/student/join_school.html`
- Modify: `templates/student/my_classes.html` (add school join section above class join)
- Modify: `app.py` (add `join_school` route)

**Spec:** Students must join a school (via school join code) before they can join any class in that school. Joining a school creates a `SchoolMembership` row. The join flow: student enters a school join code → server finds the school → creates membership → redirects back to My Classes. The class join form stays but now validates that the student is a member of the class's school before enrolling.

- [ ] **Step 1: Add `join_school` route**

```python
@app.route('/join-school', methods=['POST'])
@login_required
def join_school():
    code = request.form.get('school_code', '').strip()
    school = School.query.filter_by(join_code=code).first()
    if not school:
        flash('Invalid school join code.', 'danger')
        return redirect(url_for('my_classes'))
    exists = SchoolMembership.query.filter_by(
        school_id=school.id, user_id=current_user.id
    ).first()
    if exists:
        flash(f'You are already a member of "{school.name}".', 'info')
        return redirect(url_for('my_classes'))
    db.session.add(SchoolMembership(
        school_id=school.id,
        user_id=current_user.id,
        role='student',
    ))
    db.session.commit()
    flash(f'Joined school "{school.name}"! Now you can join classes at that school.', 'success')
    return redirect(url_for('my_classes'))
```

- [ ] **Step 2: Update `join_class` route to check school membership**

In the existing `join_class` route, after finding the class by join code, add:

```python
    klass = Class.query.filter_by(join_code=code).first()
    if not klass:
        flash('Invalid class code.', 'danger')
        return redirect(url_for('my_classes'))
    # school membership check
    if klass.course and klass.course.school_id:
        mem = SchoolMembership.query.filter_by(
            school_id=klass.course.school_id,
            user_id=current_user.id,
        ).first()
        if not mem:
            school = School.query.get(klass.course.school_id)
            flash(
                f'You must join school "{school.name}" first. '
                f'Ask your teacher for the school join code.',
                'warning'
            )
            return redirect(url_for('my_classes'))
```

- [ ] **Step 3: Update `templates/student/my_classes.html`** — add school join section above class join

```html
  <div class="card mb-3">
    <div class="card-header fw-semibold">Join a school</div>
    <div class="card-body">
      <p class="text-muted small mb-2">You need to join a school before you can join its classes.</p>
      <form method="post" action="{{ url_for('join_school') }}" class="d-flex gap-2" style="max-width:360px">
        <input type="text" name="school_code" class="form-control" placeholder="School join code" required>
        <button type="submit" class="btn btn-outline-secondary">Join School</button>
      </form>
    </div>
  </div>

  <div class="card mb-4">
    <div class="card-header fw-semibold">Join a class</div>
    ...
```

Keep the existing class join card below it.

- [ ] **Step 4: Update `my_classes` route to also pass school memberships**

```python
@app.route('/my-classes')
@login_required
def my_classes():
    school_memberships = SchoolMembership.query.filter_by(
        user_id=current_user.id
    ).all()
    return render_template('student/my_classes.html',
                           classes=current_user.classes,
                           school_memberships=school_memberships)
```

Optionally display which schools the student belongs to in `my_classes.html` below the join-school card:
```html
{% if school_memberships %}
<p class="text-muted small mb-3">
  Your schools: {% for m in school_memberships %}<strong>{{ m.school.name }}</strong>{% if not loop.last %}, {% endif %}{% endfor %}
</p>
{% endif %}
```

- [ ] **Step 5: Verify manually**

Try to join a class that has a course assigned to a school, without being a school member. Should get warning flash. Then join the school, then join the class — succeeds.

- [ ] **Step 6: Commit**

```bash
git add app.py templates/student/my_classes.html
git commit -m "feat: school join flow — must join school before joining a class"
```

---

### Task 9: Class teacher assignment in class management view

**Files:**
- Modify: `templates/teacher/edit_class.html`
- Modify: `app.py` (add `teacher_assign_class_teacher` route)

**Spec:** In the class edit view (for admin/admin_teacher), add a section to assign a `class_teacher` to the class. Shows current assigned teacher and a dropdown of users with `class_teacher` role (or all school members who could be made class_teacher).

- [ ] **Step 1: Add route**

```python
@app.route('/teacher/classes/<int:class_id>/assign-teacher', methods=['POST'])
@login_required
@role_required('admin_teacher', 'admin')
def teacher_assign_class_teacher(class_id):
    klass = Class.query.get_or_404(class_id)
    user_id = request.form.get('user_id', type=int)
    if user_id:
        user = User.query.get_or_404(user_id)
        klass.assigned_teacher_id = user.id
    else:
        klass.assigned_teacher_id = None
    db.session.commit()
    flash('Class teacher updated.', 'success')
    return redirect(url_for('teacher_edit_class', class_id=class_id))
```

- [ ] **Step 2: Update `teacher_edit_class` route to pass candidate teachers**

In `app.py`, update the `teacher_edit_class` GET handler to also query class_teachers:

```python
    class_teachers = User.query.filter_by(role='class_teacher').all()
    return render_template('teacher/edit_class.html',
                           cls=cls,
                           courses=courses,
                           modules_with_exercises=modules_with_exercises,
                           hidden_ids=hidden_ids,
                           overrides=overrides,
                           class_teachers=class_teachers)
```

- [ ] **Step 3: Add assignment card to `edit_class.html`**

After the "Class Settings" card, add:

```html
<div class="card mb-4">
  <div class="card-header fw-semibold">Assign Class Teacher</div>
  <div class="card-body">
    <form method="post" action="{{ url_for('teacher_assign_class_teacher', class_id=cls.id) }}"
          class="d-flex gap-2 align-items-center">
      <select name="user_id" class="form-select" style="max-width:280px">
        <option value="">— No assigned teacher —</option>
        {% for t in class_teachers %}
        <option value="{{ t.id }}" {% if cls.assigned_teacher_id == t.id %}selected{% endif %}>
          {{ t.display_name or t.email }}
        </option>
        {% endfor %}
      </select>
      <button type="submit" class="btn btn-outline-primary">Assign</button>
    </form>
    {% if cls.assigned_teacher %}
    <p class="text-muted small mt-2">
      Currently assigned: <strong>{{ cls.assigned_teacher.display_name or cls.assigned_teacher.email }}</strong>
    </p>
    {% endif %}
  </div>
</div>
```

- [ ] **Step 4: Commit**

```bash
git add app.py templates/teacher/edit_class.html
git commit -m "feat: class teacher assignment in class edit view"
```

---

### Task 10: Admin teacher home + user management (full admin)

**Files:**
- Modify: `templates/home.html` + `app.py` `home` route (teacher home view)
- Create: `templates/admin/users.html`
- Modify: `app.py` (add `admin_users`, `admin_delete_user` routes; update `home` route)

**Spec:**
- **Teacher/admin_teacher home**: same as student home but shows all courses in their school (not student's classes). `user_classes` = all courses in the admin_teacher's school, displayed as course cards linking to `/admin/courses/<id>/classes`.
- **Full admin**: same as admin_teacher home (sees courses for all schools? or just show sandbox). Keep it simple: admin sees the sandbox mode home (no school filter).
- **User management** (`/admin/users`): table of all users — email, display name, role, created_at — with Delete button. Full admin only.

- [ ] **Step 1: Update `home` route in `app.py`**

```python
@app.route('/')
@login_required
def home():
    sandbox_mode = request.args.get('sandbox') == '1'
    user_classes = current_user.classes if current_user.role == 'student' else []
    teacher_courses = []
    if current_user.role == 'admin_teacher' and not sandbox_mode:
        mem = SchoolMembership.query.filter_by(
            user_id=current_user.id, role='admin_teacher'
        ).first()
        if mem:
            teacher_courses = Course.query.filter_by(
                school_id=mem.school_id
            ).all()
    return render_template('home.html',
                           user_classes=user_classes,
                           teacher_courses=teacher_courses,
                           sandbox_mode=sandbox_mode)
```

- [ ] **Step 2: Update `templates/home.html` to show teacher course cards**

After the opening `{% if user_classes and not sandbox_mode %}` block, add an `{% elif teacher_courses and not sandbox_mode %}` branch:

```html
{% elif teacher_courses and not sandbox_mode %}
{# ── TEACHER HOME — COURSES IN SCHOOL ──────────────────────────────────── #}
    <div class="text-center mb-5">
      <h1 class="fw-bold display-5">Musicianship Trainer</h1>
      <p class="text-muted lead">School courses — default coursework.</p>
    </div>
    <div class="row g-4 mb-4 {% if teacher_courses | length == 1 %}justify-content-center{% endif %}">
      {% for course in teacher_courses %}
      <div class="col-md-6">
        <a href="{{ url_for('admin_course_classes', course_id=course.id) }}" class="text-decoration-none">
          <div class="card shadow-sm h-100 border-0 home-card">
            <div class="card-body p-4 text-center">
              <h2 class="h4 fw-bold mb-2">{{ course.name }}</h2>
              <p class="text-muted mb-0">{{ course.classes | length }} class{{ 'es' if course.classes | length != 1 }}</p>
            </div>
            <div class="card-footer bg-primary text-white text-center fw-semibold py-2">
              View →
            </div>
          </div>
        </a>
      </div>
      {% endfor %}
    </div>
    <div class="text-center mt-2">
      <a href="{{ url_for('home', sandbox='1') }}" class="btn btn-outline-secondary btn-sm">Open Sandbox</a>
    </div>
```

- [ ] **Step 3: Add user management routes**

```python
@app.route('/admin/users')
@login_required
@role_required('admin')
def admin_users():
    users = User.query.order_by(User.created_at.desc()).all()
    return render_template('admin/users.html', users=users)


@app.route('/admin/users/<int:user_id>/delete', methods=['POST'])
@login_required
@role_required('admin')
def admin_delete_user(user_id):
    if user_id == current_user.id:
        flash('Cannot delete your own account.', 'danger')
        return redirect(url_for('admin_users'))
    user = User.query.get_or_404(user_id)
    db.session.delete(user)
    db.session.commit()
    flash(f'Deleted account {user.email}.', 'success')
    return redirect(url_for('admin_users'))
```

- [ ] **Step 4: Create `templates/admin/users.html`**

```html
{% extends 'base.html' %}
{% block title %}Admin — Users{% endblock %}
{% block content %}
<div class="container py-4">
  <h2>Users</h2>
  <a href="{{ url_for('admin') }}" class="btn btn-outline-secondary btn-sm mb-3">← Admin Home</a>
  <table class="table table-sm align-middle">
    <thead>
      <tr><th>Email</th><th>Name</th><th>Role</th><th>Joined</th><th></th></tr>
    </thead>
    <tbody>
    {% for u in users %}
    <tr>
      <td>{{ u.email }}</td>
      <td>{{ u.display_name or '—' }}</td>
      <td><span class="badge bg-secondary">{{ u.role }}</span></td>
      <td class="text-muted small">{{ u.created_at.strftime('%Y-%m-%d') if u.created_at else '—' }}</td>
      <td>
        {% if u.id != current_user.id %}
        <form method="post" action="{{ url_for('admin_delete_user', user_id=u.id) }}"
              onsubmit="return confirm('Permanently delete account {{ u.email }}?')">
          <button class="btn btn-sm btn-outline-danger">Delete</button>
        </form>
        {% else %}
        <span class="text-muted small">(you)</span>
        {% endif %}
      </td>
    </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %}
```

- [ ] **Step 5: Add Users link to admin home**

In `templates/admin/index.html` (or wherever the admin home template is), add a link to `/admin/users`:

```html
<a href="{{ url_for('admin_users') }}" class="btn btn-outline-secondary">Users</a>
```

- [ ] **Step 6: Verify manually**

Log in as admin_teacher → home shows school courses. Log in as student → home shows classes as before. Visit `/admin/users` as admin → full user list with Delete buttons.

- [ ] **Step 7: Commit**

```bash
git add app.py templates/home.html templates/admin/users.html
git commit -m "feat: teacher home shows school courses; admin user management page"
```
